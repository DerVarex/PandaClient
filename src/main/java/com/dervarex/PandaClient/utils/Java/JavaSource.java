package com.dervarex.PandaClient.utils.Java;

// Placeholder to keep backward references compiling if any remain.
public interface JavaSource {
    // Intentionally empty; download logic now lives in JavaManager
}
